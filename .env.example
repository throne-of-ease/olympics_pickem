# Supabase Configuration (required for auth and database features)
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api

# Frontend (exposed to browser - use anon key, not service role)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend (Netlify Functions) - same values, different names for server-side
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Tournament selector (default: mens_ice_hockey). Options: mens_ice_hockey, womens_ice_hockey
VITE_TOURNAMENT_KEY=mens_ice_hockey
TOURNAMENT_KEY=mens_ice_hockey

# Optional: Force CSV fallback mode (disables Supabase features)
# USE_CSV_FALLBACK=true

# Optional: Use mock ESPN data instead of live API
# USE_MOCK_DATA=true
